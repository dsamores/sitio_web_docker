name: Sitio web con Docker

on: [push]

permissions:
  contents: read

jobs:
  hola:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Mensaje de saludo
        run: |
          echo "Hola mundo..."
